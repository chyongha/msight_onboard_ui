# Where the Flask/Socket.IO backend is reachable from the browser running this frontend. 
VITE_BACKEND_URL=http://127.0.0.1:5000

# Map center shown before the first alert ever arrives
VITE_DEFAULT_LAT=42.2975
VITE_DEFAULT_LON=-83.7042
